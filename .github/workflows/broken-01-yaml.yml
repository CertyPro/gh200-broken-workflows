# BROKEN ON PURPOSE - Lab 01 (invalid YAML).
# Symptom: GitHub shows "Invalid workflow file" and the workflow never runs.
# Find the bug, fix it, and the workflow should run.
#
# Hint: look very closely at the line below the workflow name.
name: Broken 01 YAML

on
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci
      - run: npm test
